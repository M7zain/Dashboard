import React from 'react'
import { Metadata } from 'next';
 
export const metadata: Metadata = {
  title: 'customers',
};

const page = () => {
  return (
    <div>customers</div>
  )
}

export default page